streamlit>=1.28.0
pandas>=1.5.0
numpy>=1.21.0
plotly>=5.0.0
openpyxl>=3.0.0
requests>=2.28.0
gspread>=5.0.0
google-auth>=2.0.0
google-auth-oauthlib>=0.5.0
google-auth-httplib2>=0.1.0

# AI/ML (for chatbot)
openai>=1.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Environment Management
python-dotenv>=1.0.0

# 제거됨 (사용 안 함):
# chromadb>=0.5.5 (백업 파일에만 존재, 약 30개 의존성)
# duckdb>=1.0,<2.0 (사용 안 함)
# google-generativeai>=0.6,<0.7 (OpenAI로 전환, 백업용으로만 필요)

